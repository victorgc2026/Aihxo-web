/* AIHXO · Recepción por código de barras + OCR + lote */
(function(){
 const norm=v=>String(v||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,' ').trim();
 const aliases={blanco:['blanco','white','blanc'],negro:['negro','black','noir'],'azul marino':['azul marino','navy','navy blue'],gris:['gris','grey','gray'],lila:['lila','lilac'],arena:['arena','sand'],'azul cielo':['azul cielo','sky blue','sky'],'verde agua':['verde agua','aqua','mint']};
 let stock=[],purchaseId=null,lastText='',lastBarcode='',batch=[];
 const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
 const close=()=>document.getElementById('aihxoRecvOverlay')?.remove();
 function shell(){close();const o=document.createElement('div');o.id='aihxoRecvOverlay';o.style.cssText='position:fixed;inset:0;z-index:99999;background:rgba(0,0,0,.55);display:flex;align-items:flex-end;justify-content:center';o.innerHTML=`<div style="background:#fff;width:100%;max-width:760px;max-height:94vh;overflow:auto;border-radius:22px 22px 0 0;padding:18px;box-sizing:border-box"><div style="display:flex;justify-content:space-between;align-items:center;gap:12px"><div><h2 style="margin:0">📦 Recepción por lote</h2><div style="opacity:.65;font-size:14px">Escanea varias prendas y confirma todo al final</div></div><button id="aihxoRecvClose" type="button" style="font-size:22px;border:0;background:transparent">✕</button></div><div id="aihxoRecvContent" style="margin-top:14px"></div></div>`;document.body.appendChild(o);o.querySelector('#aihxoRecvClose').onclick=close;o.onclick=e=>{if(e.target===o)close()}}
 async function loadOCR(){if(window.Tesseract)return;await new Promise((ok,ko)=>{const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/tesseract.js@5/dist/tesseract.min.js';s.onload=ok;s.onerror=ko;document.head.appendChild(s)})}
 async function detectBarcode(file){lastBarcode='';try{if(!('BarcodeDetector'in window)||!window.createImageBitmap)return null;const formats=await BarcodeDetector.getSupportedFormats?.()||[];const d=new BarcodeDetector({formats:formats.length?formats:undefined});const bmp=await createImageBitmap(file);const codes=await d.detect(bmp);bmp.close?.();const code=codes?.[0]?.rawValue?.trim();if(code){lastBarcode=code;return code}}catch(e){console.warn(e)}return null}
 function scoreItem(item,text){const t=norm(text),supplier=norm(item.supplier),model=norm(item.supplier_model),size=norm(item.size),color=norm(item.color);let s=0;if(supplier&&t.includes(supplier))s+=4;if(model&&t.includes(model))s+=6;if(size&&t.includes(size))s+=5;const terms=[color,...(aliases[color]||[])].map(norm).filter(Boolean);if(terms.some(x=>t.includes(x)))s+=5;return s}
 const matches=text=>stock.map(x=>({...x,_score:scoreItem(x,text)})).filter(x=>x._score>0).sort((a,b)=>b._score-a._score).slice(0,5);
 const byBarcode=code=>stock.find(x=>String(x.barcode||'').trim()===String(code||'').trim())||null;
 function renderHome(){const b=document.getElementById('aihxoRecvContent');if(!b)return;b.innerHTML=`${purchaseId?'<div style="padding:10px 12px;background:#f4f6f8;border-radius:10px;margin-bottom:10px">Recepción vinculada a una compra.</div>':''}<div style="margin-bottom:12px">Escanea las etiquetas una a una. Las prendas se irán acumulando en un lote sin modificar el stock hasta que pulses <b>Confirmar recepción</b>.</div><label for="labelPhoto" style="display:block;text-align:center;padding:15px;border-radius:12px;background:#07152f;color:#fff;font-weight:800;cursor:pointer">📷 Escanear prenda</label><input id="labelPhoto" type="file" accept="image/*" capture="environment" style="position:absolute;left:-9999px;width:1px;height:1px"><div id="ocrStatus" style="margin-top:12px;padding:14px;border:1px solid #ddd;border-radius:12px"><b>Preparado.</b></div><div id="ocrResult"></div><div id="batchBox" style="margin-top:14px"></div>`;document.getElementById('labelPhoto').onchange=async e=>{const f=e.target.files?.[0];if(f)await process(f);e.target.value=''};renderBatch()}
 function renderBatch(){const box=document.getElementById('batchBox');if(!box)return;const total=batch.reduce((a,x)=>a+x.qty,0);box.innerHTML=`<div style="display:flex;justify-content:space-between;align-items:center;gap:10px;margin-bottom:8px"><h3 style="margin:0">Lote actual</h3><b>${total} ud.</b></div>${batch.length?batch.map((x,i)=>{const s=stock.find(y=>String(y.id)===String(x.item_id));return `<div style="display:grid;grid-template-columns:1fr auto;gap:10px;padding:10px 0;border-bottom:1px solid #eee"><div><b>${esc(s?.supplier||'')} · ${esc(s?.supplier_model||'')}</b><div style="font-size:13px;opacity:.7">${esc(s?.size||'')} · ${esc(s?.color||'')} ${x.barcode?'· '+esc(x.barcode):''}</div></div><div style="display:flex;align-items:center;gap:6px"><button type="button" data-minus="${i}" style="padding:5px 9px">−</button><b>${x.qty}</b><button type="button" data-plus="${i}" style="padding:5px 9px">＋</button><button type="button" data-del="${i}" style="padding:5px 9px">✕</button></div></div>`}).join(''):'<div style="padding:14px;border:1px dashed #ccc;border-radius:12px;opacity:.65">Aún no has añadido prendas.</div>'}${batch.length?`<button id="confirmBatch" type="button" style="width:100%;margin-top:14px;padding:15px;border:0;border-radius:12px;background:#0a7f32;color:white;font-weight:900">✅ Confirmar recepción (${total} ud.)</button><button id="clearBatch" type="button" style="width:100%;margin-top:8px;padding:12px;border:1px solid #ccc;border-radius:12px;background:#fff;font-weight:700">Vaciar lote</button>`:''}`;box.querySelectorAll('[data-minus]').forEach(b=>b.onclick=()=>{const i=+b.dataset.minus;batch[i].qty=Math.max(1,batch[i].qty-1);renderBatch()});box.querySelectorAll('[data-plus]').forEach(b=>b.onclick=()=>{batch[+b.dataset.plus].qty++;renderBatch()});box.querySelectorAll('[data-del]').forEach(b=>b.onclick=()=>{batch.splice(+b.dataset.del,1);renderBatch()});document.getElementById('clearBatch')?.addEventListener('click',()=>{if(confirm('¿Vaciar el lote actual?')){batch=[];renderBatch()}});document.getElementById('confirmBatch')?.addEventListener('click',confirmBatch)}
 async function process(file){const st=document.getElementById('ocrStatus');lastText='';lastBarcode='';st.innerHTML='<b>🔎 Buscando código de barras…</b>';try{const code=await detectBarcode(file);if(code){const exact=byBarcode(code);if(exact){st.innerHTML=`<b>✅ Código reconocido: ${esc(code)}</b>`;showCandidate(exact,true);return}st.innerHTML=`<b>✅ Código nuevo: ${esc(code)}</b><div style="opacity:.65">Leyendo texto para identificar la variante…</div>`}else st.innerHTML='<b>🔎 Leyendo texto de etiqueta…</b>';await loadOCR();const r=await Tesseract.recognize(file,'eng+spa',{logger:m=>{if(m.status==='recognizing text')st.innerHTML=`<b>🔎 Leyendo etiqueta… ${Math.round((m.progress||0)*100)}%</b>`}});lastText=r?.data?.text||'';const ms=matches(lastText);if(!ms[0]){document.getElementById('ocrResult').innerHTML='<div style="margin-top:12px;padding:14px;border:1px solid #f0b7b7;border-radius:12px">⚠️ No encontré una variante clara.</div>';return}showCandidate(ms[0],false,ms)}catch(e){console.error(e);st.innerHTML='<b>⚠️ No se pudo leer la etiqueta.</b>'}}
 function showCandidate(x,known=false,ms=[]){const r=document.getElementById('ocrResult');r.innerHTML=`<div style="margin-top:12px;padding:14px;border:1px solid #ddd;border-radius:12px"><h3 style="margin-top:0">${known?'Variante reconocida':'Coincidencia propuesta'}</h3><div style="font-size:18px;font-weight:800">${esc(x.supplier||'')} · ${esc(x.supplier_model||'')}</div><div style="opacity:.7">${esc(x.size||'')} · ${esc(x.color||'')}</div>${lastBarcode?`<div style="font-size:12px;margin-top:5px">Código: <b>${esc(lastBarcode)}</b></div>`:''}<label style="display:block;margin-top:10px">Unidades<input id="recvQty" type="number" min="1" value="1" style="display:block;width:100%;box-sizing:border-box;padding:12px;margin-top:5px"></label><button id="addBatch" type="button" style="width:100%;margin-top:12px;padding:14px;border:0;border-radius:12px;background:#07152f;color:#fff;font-weight:800">＋ Añadir al lote</button>${ms.length>1?`<details style="margin-top:10px"><summary>Elegir otra coincidencia</summary><div id="altMatches"></div></details>`:''}</div>`;document.getElementById('addBatch').onclick=()=>addToBatch(x.id);const alt=document.getElementById('altMatches');if(alt)ms.slice(1).forEach(y=>{const b=document.createElement('button');b.type='button';b.textContent=`${y.supplier||''} · ${y.supplier_model||''} · ${y.size||''} · ${y.color||''}`;b.style.cssText='width:100%;margin-top:7px;padding:10px';b.onclick=()=>showCandidate(y,false,[]);alt.appendChild(b)})}
 function addToBatch(id){const qty=Math.max(1,Number(document.getElementById('recvQty')?.value||1));const existing=batch.find(x=>String(x.item_id)===String(id));if(existing)existing.qty+=qty;else batch.push({item_id:id,qty,barcode:lastBarcode||null});document.getElementById('ocrResult').innerHTML='<div style="margin-top:12px;padding:12px;border:1px solid #b7d8bd;border-radius:12px"><b>✅ Añadido al lote</b></div>';renderBatch();setTimeout(()=>document.getElementById('labelPhoto')?.click(),300)}
 async function confirmBatch(){if(!batch.length)return;const btn=document.getElementById('confirmBatch');btn.disabled=true;btn.textContent='Guardando…';try{const payload=batch.map(x=>({item_id:x.item_id,qty:x.qty,barcode:x.barcode||null}));const {data,error}=await supabaseClient.rpc('receive_stock_batch',{p_items:payload,p_purchase_id:purchaseId||null});if(error)throw error;batch.forEach(x=>{const s=stock.find(y=>String(y.id)===String(x.item_id));if(s){s.quantity=Number(s.quantity||0)+x.qty;if(x.barcode&&!s.barcode)s.barcode=x.barcode}});const total=batch.reduce((a,x)=>a+x.qty,0);batch=[];document.getElementById('ocrResult').innerHTML='';document.getElementById('ocrStatus').innerHTML=`<b>✅ Recepción registrada</b><div style="opacity:.65">${total} unidades añadidas al stock.</div>`;renderBatch()}catch(e){console.error(e);alert(e.code==='23505'?'Uno de los códigos de barras ya está asociado a otra variante.':'No se pudo registrar el lote completo. No se ha aplicado parcialmente.');btn.disabled=false;renderBatch()}}
 window.abrirRecepcionEtiquetas=async function(pid=null){purchaseId=pid||null;batch=[];shell();const box=document.getElementById('aihxoRecvContent');box.innerHTML='<div style="padding:16px;border:1px solid #ddd;border-radius:12px"><b>Cargando stock…</b></div>';try{const {data,error}=await supabaseClient.from('base_stock_items').select('*').order('supplier_model').order('color').order('size');if(error)throw error;stock=data||[];renderHome()}catch(e){console.error(e);box.innerHTML='<div style="padding:16px;border:1px solid #f0b7b7;border-radius:12px"><b>⚠️ No se pudo cargar el stock.</b></div>'}};
 document.addEventListener('click',e=>{const main=e.target.closest?.('#recvCamera');if(main){e.preventDefault();e.stopPropagation();window.abrirRecepcionEtiquetas(null);return}const row=e.target.closest?.('.recvPurchase');if(row){e.preventDefault();e.stopPropagation();window.abrirRecepcionEtiquetas(row.dataset.purchaseId||null)}},true)
})();